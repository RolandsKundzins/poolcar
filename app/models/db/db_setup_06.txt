CREATE TABLE PASSENGER(
	USER_ID VARCHAR(32),
    COORDINATES VARCHAR(128),
    RADIUS VARCHAR(32),
    DATE DATETIME,
    STATUS VARCHAR(8),
    CONSTRAINT PK_PASSENGER PRIMARY KEY (USER_ID, DATE)
);